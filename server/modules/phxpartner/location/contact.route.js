module.exports = function (app) {
    var contactCtrl = app.modules.phxpartner.location.contactCtrl;

    // app.get('/api/phxpartner/location/:id/contact',
    //     contactCtrl.getLocationContact
    // );

    // app.put('/api/phxpartner/location/:id/contact',
    //     contactCtrl.updLocationContact
    // );
}