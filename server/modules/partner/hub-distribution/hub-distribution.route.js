module.exports = function (app) {
    var hubDistributionCtrl = app.modules.partner.hubDistribution.hubDistributionCtrl;

    // app.get('/api/phxpartner/hub-distribution/info',
    //     hubDistributionCtrl.getHubDistributionInfo
    // );

    // app.get('/api/phxpartner/hub-distribution/list',
    //     hubDistributionCtrl.getHubDistributionList
    // );
  
};